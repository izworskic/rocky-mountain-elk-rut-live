{
  "name": "rocky-mountain-elk-rut-live",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify:static": "node scripts/verify-static.mjs",
    "verify": "npm test && npm run verify:static",
    "vercel-build": "npm run verify"
  },
  "engines": {
    "node": "22.x"
  }
}
